package org.jodaengine.eventmanagement.adapter;

/**
 * Testing basic functionality of the CorrelationEventAdapter.
 */
public class CorrelationEventAdapterTest {

//  Assert.assertEquals(eventManager.getEventAdapters().size(), 1);
//  AbstractCorrelatingEventAdapter<?> mailEventAdapter = (AbstractCorrelatingEventAdapter<?>) eventManager.getEventAdapters().values().iterator().next();
//  Assert.assertEquals(mailEventAdapter.getProcessEvents().size(), 1);
//  Assert.assertEquals(startEvent, mailEventAdapter.getProcessEvents().get(0));
}
